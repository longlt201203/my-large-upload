{
  "name": "api-cluster",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "api-cluster/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "api-cluster:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "api-cluster:build:development"
        },
        "production": {
          "buildTarget": "api-cluster:build:production"
        }
      }
    }
  }
}
